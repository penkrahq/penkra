# App Account identity

The complete public `account-identity` contract now lives in the single App-author guide:
[App development: `account-identity`](./app-development.md#account-identity).

That section includes the manifest and SDK contract, automatic development sideload identity,
token claims, a backend verifier example, authorization requirements, failure handling, JWKS
caching, and key rotation. This compatibility pointer contains no additional contract.
